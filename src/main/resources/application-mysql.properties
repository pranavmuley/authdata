database=mysql

# Delete the rest of the original content of the file and replace with the following:
spring.cloud.gcp.sql.database-name=merchant_auth_data_db
spring.cloud.gcp.sql.instance-connection-name=pid-gousenaib-aini-res01:us-central1:merchant-auth-data
spring.datasource.continue-on-error=true

spring.datasource.username=root

spring.datasource.password=root

spring.cloud.gcp.project-id=pid-gousenaib-aini-res01
# Initialize the database since the newly created Cloud SQL database has no tables. The following flag is for Spring Boot 2.
spring.datasource.initialization-mode=always
